jest.dontMock '../store'

Schema = require('../schema')
Store  = require('../store')

describe 'Store', ->
  # it '#registerFn', ->
  # it '#events', ->

  # it '#create', ->
  # it '#update', ->
  # it '#destroy', ->
  # it '#read', ->
